import java.io.IOException;

import javassist.CannotCompileException;
import javassist.NotFoundException;

public class Test {
	public static void main(String[] args){
		/*StudentInfoService studentInfo = null;
		try {
			studentInfo = (StudentInfoService) MyProxyImpl.newProxyInstance("StudentInfoService", "StudentInfoServiceImpl", "InterceptorHandlerImpl");
			studentInfo.findInfo("阿甘", 999);
			StudentInfoService temp = studentInfo.getNo(2, studentInfo);
			float index = studentInfo.say().floatValue();
			System.out.println("index " + index);
			temp.findInfo("aaa", 100);
		} catch (InstantiationException e) {
			e.printStackTrace();
		} catch (IllegalAccessException e) {
			e.printStackTrace();
		} catch (NotFoundException e) {
			e.printStackTrace();
		} catch (CannotCompileException e) {
			e.printStackTrace();
		} catch (ClassNotFoundException e) {
			e.printStackTrace();
		} catch (IOException e) {
			e.printStackTrace();
		}*/
	}
}
